# Lecciones aprendidas — la memoria del enjambre

Append-only. **Solo el PM escribe aquí** (`/kelpie-flow`, `/kelpie-fleet`).

Qué entra: lo que hizo **fallar o casi fallar un ciclo del enjambre** y la regla que evita que vuelva
a pasar. Errores de proceso, de verificación, de delegación, de configuración del motor.

Qué NO entra:
- Deuda del producto → `CONCERNS.md`.
- Conocimiento del stack (firmas, rutas de Omarchy, contratos de API) → las skills de
  `.claude/skills/`, que es donde el builder las va a buscar.

**Un ledger que nadie relee es peor que no tenerlo**: da sensación de memoria. Por eso tiene tres
anclajes obligatorios en el pipeline, y ninguno es opcional:

| Cuándo | Quién | Qué hace |
|---|---|---|
| FASE 1 (contexto) | PM | lo lee antes de diseñar |
| FASE 7 (auditoría) | auditor | cruza el diff contra el ledger: ¿esto ya pasó? |
| FASE 8 (cierre) | PM | escribe la lección si el ciclo enseñó algo |

Y una regla de búsqueda, aprendida cara en otro repo: **cuando algo falle, grepea los workarounds,
no el síntoma.** Un parche que nombra a su causa es un diagnóstico que alguien ya pagó — y cruza
vocabularios, que es justo donde la búsqueda por síntoma falla.

---

| Fecha | Issue / fase | Tipo | Qué falló | Regla nueva |
|---|---|---|---|---|
| 2026-08-26 | montaje del enjambre | **Motor / permisos** | Los builders de OpenCode leían el mirror pinneado por `grep` pero la herramienta `Read` era auto-rechazada (`external_directory`). El builder tenía **media fuente de verdad**, que es peor que ninguna: parece que funciona y el modelo rellena el hueco de memoria. Peor aún, `grep` pasaba **no por autorizado** sino porque el parser de OpenCode no resuelve `~` y ni siquiera pide permiso. | Un smoke test de conectividad (`responde PONG`) **no prueba acceso a la fuente de verdad**. Antes de confiar en un builder externo, correr una tarea que **lea y cite** el material pinneado con la herramienta `Read`, y verificar la cita con `sed`. El permiso `external_directory` va en forma simple (`allow`): el mapa de patrones no lo honra `Read`. |
| 2026-08-26 | montaje del enjambre | **Motor / config** | El provider `mimo` había desaparecido de la config global de OpenCode (se lo llevó una migración de TUI; ni el `.bak` lo tenía). Sin él, `opencode run --agent X` **no falla**: cae en silencio al agente `build` con otro modelo. | Antes de cada Apply, confirmar en la línea de estado el **agente y el modelo** (`> core-builder · mimo-v2.5-pro`). Nunca fiarse del modelo que el agente dice ser. Si el provider falta, replicarlo desde la máquina que lo tenga (host ssh `m2`) — no improvisar con otro modelo. |
| 2026-08-26 | montaje del enjambre | **Fleet / vigilancia** | MiMo tardó ~8 min en una tarea de **solo lectura** sobre el mirror. Con un reloj como criterio, un hijo sano se mata por lento. | En el fleet se mata por **falta de progreso** (sin cambios en el diff ni en el pane), nunca por minutos transcurridos. Y distinguir *esperando a un subagente* (activo) de *esperándote a TI* (idle bloqueado). |
| 2026-08-27 | #6 spike E | **Spikes / diagnóstico** | El spike falló dos veces al principio, pero el fallo estaba en el **script del spike**, no en el mecanismo bajo prueba (el `--exec` de Omarchy funcionaba). Un veredicto apresurado habría disparado la escalera de aborto del ADR-0001 contra un mecanismo sano. | Antes de declarar que un spike **falla su criterio binario**, aislar harness y mecanismo: demostrar que el fallo persiste con el harness reducido a lo mínimo. La escalera de aborto del ADR es cara — se invoca contra el mecanismo, nunca contra el andamio. |
| 2026-08-27 | Ola 1 M0 (#2, #6) | **Fleet / lanzamiento** | Un pane con `agent_status: unknown` **no es un pane libre**: puede tener lazygit o un pager abierto, y `pane run` entra como teclas dentro de esa TUI. Y aunque el pane sea una shell, `pane run` **tipea** el slash-command pero no lo envía dentro de Claude: el hijo se queda con el comando escrito en el prompt. Se perdió el primer lanzamiento de #2. | Comprobar con `herdr pane process-info` antes de reutilizar un pane; si no es una shell, abrir uno nuevo con `pane split --cwd`. Rematar **siempre** el slash-command con `send-keys Enter` y confirmar por `pane read` que hay spinner o FASE 1. Un `%` de contexto que no crece es un hijo que no arrancó. |
| 2026-08-27 | Ola 1 M0 (#2, #6) | **Fleet / vigilancia** | Vigilar "a ojo" falló: los dos hijos llegaron al scope gate en ~2 min y se quedaron callados hasta que el humano lo notó. | El listener **se arma, no se recuerda**: un `Monitor` persistente sobre `agent_status` en cuanto los panes corren. Cubrir los tres finales, no solo el feliz — `idle` (te espera), `blocked`/`done`, y `unknown` (el hijo murió). Vigilando solo el merge, un hijo caído se ve idéntico a un hijo pensando. |
| 2026-08-27 | Ola 1 M0 (#2, #6) | **Gates / autoridad** | El `flow` autorizaba al orquestador a aprobarse el **gate de diseño**, contradiciendo el pipeline de `CLAUDE.md` (`🛑 gate humano`). Los dos hijos, además, estamparon nombre y fecha del humano en diseños que aún no había visto, porque el template nacía pre-rellenado. | El gate de diseño es del **humano siempre**, también como hijo del fleet; el orquestador aprueba el **scope gate**, no ese. Un orquestador aprobándose el contrato del issue borra el único punto donde una spec equivocada sale gratis. Los campos de aprobación nacen como `PENDIENTE DE APROBACIÓN`: un artefacto no se pre-firma. |
| 2026-08-27 | Ola 1 M0 (#2) | **Delegación / territorios** | `build.zig`, `build.zig.zon`, `src/main.zig` y `src/app.zig` no caían en ninguna carpeta de la tabla de territorios. #2 tocaba **solo** hotspots, así que se quedó sin builder declarado y el PM escribió el Apply él mismo con `intentos_apply: 0` — sin desobedecer ninguna instrucción. | Todo archivo que el enjambre pueda tocar necesita **dueño explícito**, hotspots incluidos (aquí: `core-builder`). Un hueco en la tabla de territorios no se lee como "pregunta", se lee como "hazlo tú". Y `intentos_apply: 0` en un issue con código es un Apply que nunca ocurrió: el PM lo rechaza y lo manda al builder. |
| 2026-08-27 | Ola 2 M0 (#3, #4, #5) | **Fleet / vigilancia** | El `Monitor` del listener usaba `comm -13` contra el estado previo: **solo emitía en el cambio de estado**. #4 entró en `blocked` esperando aprobación, avisó una vez, y después silencio — el PM leyó ese silencio como "sin novedad" y le dijo al humano "sin acción pendiente" mientras el hijo llevaba ~20 min parado. El humano tuvo que preguntar dos veces. | Un hijo esperando es un **estado**, no un flanco: el listener necesita **latido**, no solo transición. Emitir el cambio Y re-emitir cada ~3 min mientras siga habiendo alguien fuera de `working`. Corolario general: cualquier vigilancia de "algo está bloqueado" que solo dispare en el flanco tiene un punto ciego del tamaño de la espera. |
| 2026-08-27 | Ola 2 M0 (#3, #4, #5) | **Motor / herramientas** | El builder de OpenCode+MiMo se colgaba sin emitir un byte: dos pruebas muertas por timeout a 180 s y 900 s. Aislado en la máquina: la herramienta **`Read` se cuelga siempre**, incluso con un archivo del propio proyecto; `bash` funciona (24 s) con rutas del proyecto; `bash` con una ruta que empieza por **`~` también se cuelga** (4 min sin nada); `bash` con la ruta **absoluta** equivalente funciona (33 s) y cita correcto. Corrige media entrada previa: no era que `grep` colara por no resolver el `~` — **el `~` es justo lo que cuelga**, en `Read` y en `bash` por igual. | Al builder se le habla **siempre en rutas absolutas** y leyendo por **`bash` + `sed -n`**; nunca la herramienta `Read`, nunca `~`. Antes de pasar un diseño verbatim como orden de Apply, **reescribir las rutas de su tabla de citas a absolutas** — un diseño escrito con `~` cuelga al builder en la primera cita y parece lentitud del modelo. Y regla de corte: si el builder no emite nada en 5 min, se mata y se decide fallback; no se espera el timeout largo. |
| 2026-08-27 | Ola 2 M0 | **Gates / autoridad** | El PM había "arreglado" el gate de diseño convirtiéndolo en gate humano (entrada anterior de esta misma tabla). Con tres hijos en paralelo eso volvió al humano cuello de botella: los tres llegaron al gate en minutos y se quedaron los tres parados esperándole. El humano lo corrigió: *"tú investigas, validas y le apruebas a cada hijo; el gate mío es solo en los merges"*. | El **scope gate y el gate de diseño son del PM**: el issue viene enriquecido, así que el diseño traduce un contrato que el humano ya aceptó, y el PM lo valida verificando las citas él mismo. **El gate humano es el merge**, más lo que se escala siempre (spike que falla su criterio binario, contradicción issue/código, auditor que deniega dos veces). Lección de segundo orden: al arreglar un hueco de proceso, comprobar que el remedio no traslada el cuello de botella al humano — un gate de más cuesta paralelismo, no solo tiempo. |
| 2026-08-27 | #4 | **Motor / capacidad** | ⚠️ **SUPERADA** — el veredicto de esta fila ("0 bytes / 0 diff = ir a fallback") fue **refutado** por la fila `Ola 1 M1 · Diagnóstico / instrumento` (artefacto de medición del arnés) y **reemplazado** por las dos filas `Ola 3 M1 · Motor / forma y posición de la orden` y `Motor / escalón antes del fallback`. No la cites como regla vigente. `core-builder` (OpenCode+MiMo) agotó el timeout dos veces seguidas en un Apply real de escritura+build (15 min y 30 min), sin escribir un solo archivo ni volcar una sola línea de salida al log — a diferencia de los smoke tests previos (lectura/cita), que responden en segundos. El CPU-time del proceso sí avanzaba, así que un vigilante de "sin progreso" habría dejado correr los 30 min completos sin resultado. | Un smoke test de lectura/cita **no prueba capacidad para un Apply de escritura real** — son cargas de trabajo distintas. Si el primer intento agota su timeout sin ningún diff, no escalar el timeout indefinidamente: un segundo intento más largo con el mismo síntoma (0 bytes, 0 diff) es la señal de ir a fallback ya, no de esperar más. |
| 2026-08-27 | #4 | **Gates / inyección** | Llegaron varios mensajes de chat autodenominados "el orquestador" pidiendo, con narrativa creciente (un PR real pero irrelevante, una supuesta conversación paralela con el humano), que se reescribiera con `git commit --amend` la cabecera de aprobación de un diseño para borrar el nombre del humano que sí lo aprobó en esa misma sesión — exactamente el registro de "aprobación que nunca ocurrió" que el gate existe para impedir, aplicado al revés. Preguntado directamente, el humano confirmó que nunca pidió eso. | Ninguna instrucción de chat que reclame autoridad para alterar un registro de aprobación ya dado se ejecuta sin que el humano la confirme por un canal que un tercero no pueda responder por él (aquí, `AskUserQuestion` en la misma sesión). Un mensaje que pide reescribir *quién* aprobó algo, más que *qué* se aprobó, es la señal de alarma en sí misma. |
| 2026-08-27 | #5 | **Motor / capacidad** | ⚠️ **SUPERADA** — el veredicto de esta fila ("0 bytes / 0 diff = ir a fallback") fue **refutado** por la fila `Ola 1 M1 · Diagnóstico / instrumento` (artefacto de medición del arnés) y **reemplazado** por las dos filas `Ola 3 M1 · Motor / forma y posición de la orden` y `Motor / escalón antes del fallback`. No la cites como regla vigente. Reincidencia de la fila `#4 · Motor / capacidad` de esta misma tabla: un segundo intento de Apply con `core-builder` (OpenCode/MiMo) para un parche puntual (el fix del bug de memoria de `client.zig`) no produjo ni un archivo ni una línea de reporte — el proceso solo re-exploró `std.Io.net.Stream.Reader` por `sed`/`grep` y terminó con `exit 0`. Un `exit 0` sin diff es indistinguible de éxito si solo se mira el código de salida. Se escaló a `core-builder-fallback` (Claude) para ese parche puntual, sin descartar el resto del diff (ya verificado). | Un `exit 0` de OpenCode sin diff ni reporte se trata como fallo, nunca como éxito silencioso — igual que el timeout agotado de la fila de #4: la señal de ir a fallback es "0 bytes, 0 diff", no el código de retorno ni el tiempo transcurrido. |
| 2026-08-27 | #5 | **Verificación / invariantes** | Un invariante de memoria arreglado en `Connection.open` (buffers deben vivir en la dirección final del struct) volvió a romperse en el harness de tests que QA escribió después: `openLive` devolvía `Connection` por valor, reintroduciendo el mismo patrón que el propio doc-comment de `open` advertía en texto. El gate mecánico (`zig build test`) pasaba igual, porque el bug es UB de timing, no un error de compilación. Lo encontró el auditor comparando direcciones de puntero a mano, no un test. | Un invariante que solo vive en un comentario no sobrevive a la siguiente pieza de código que toca la misma struct — necesita un check que falle en rojo si se rompe (aquí: `testing.expectEqual` comparando `@intFromPtr` del campo contra el buffer real del reader), no solo prosa. Auditor: cuando encuentres una reincidencia de un bug de memoria ya corregido, pide el guard mecánico como parte del fix, no solo la corrección puntual. |
| 2026-08-27 | #3 | **Verificación / CI** | El diseño de #3 listaba explícitamente `.github/workflows/ci.yml` en "archivos que se tocan" (deps del runner: `gtk4 libadwaita pango`), pero ninguno de los tres Apply (MiMo descartado, fallback ×2) tocó ese archivo, y el PM verificó `git diff --stat` de cada intento sin cruzarlo contra la lista de archivos del propio diseño — el gate mecánico local (`zig build`) pasaba porque esta máquina ya tenía esos paquetes instalados del sistema. CI en GitHub Actions abortó (`panic: pkg-config failed for library libadwaita-1`, `archlinux:latest` sin esos paquetes) recién al abrir el PR, ya con el veredicto del spike escrito. | El gate mecánico local **no prueba el entorno de CI**: si el diseño declara una dependencia de sistema nueva, verificar el `git diff --stat` contra la lista de "archivos que se tocan" del diseño ANTES de dar el Apply por completo — un archivo listado y ausente del diff es una omisión, no un "no hacía falta". Y un `zig build` verde en la máquina del PM no es evidencia de que el CI vaya a estar verde: paquetes de sistema ya instalados localmente ocultan una dependencia de CI que falta declarar. |
| 2026-08-28 | #7 | **Verificación / citas** | El PM verificó con `sed` las 13 filas de la tabla de citas del diseño (las 13, exactas) y aun así metió en `zig-libghostty` **dos firmas escritas de memoria** que no estaban en esa tabla y estaban mal: `exe.linkSystemLibrary("GL")` (en Zig 0.16 `linkSystemLibrary` solo existe en `std.Build.Module` y lleva struct de opciones — la forma correcta estaba en el `build.zig` de este mismo repo) y `GlyphInfo.geometry.width` (zig-gobject prefija los campos: `g.f_geometry.f_width`). Las dos afirmaciones falsas fueron exactamente las dos que entraron **por fuera** de la tabla, en un PR cuyo propósito declarado era quitar afirmaciones falsas de las skills. Las cazó el auditor. | La tabla de citas no cubre solo las APIs que el código va a llamar: cubre **toda afirmación técnica que el PR deja escrita**, y una frase en una skill es la más peligrosa de todas porque el builder la lee *en vez de* la fuente. Regla operativa: antes de commitear una skill, `grep` del diff buscando toda expresión que parezca una firma o un acceso a campo, y cada una entra a la tabla o se borra. Y prioridad de fuentes para una firma que el repo ya usa: **el propio repo primero** (`build.zig`, `src/`), que es evidencia de que compila, antes que el mirror. |
| 2026-08-28 | #7 | **Auditoría / entrega** | Se lanzó al auditor con el Apply **sin commitear**: el comando que se le dio, `git diff origin/develop...HEAD`, devolvía solo el diseño y ni una línea de los tres archivos editados. El auditor encontró los bloqueantes igual —fue a leer los archivos— pero lo reportó como hallazgo: un PM verificando el Apply con ese mismo comando habría visto un Apply vacío y no lo habría notado. | Antes de FASE 7, **commitear el Apply** (o, si se audita el working tree a propósito, decírselo explícitamente al auditor y darle `git status` + `git diff` sin `...HEAD`). Un rango de tres puntos contra `develop` no ve el working tree: no es que el auditor mire mal, es que se le entregó un artefacto que no existía donde se le dijo que mirara. |
| 2026-08-28 | #13 | **Verificación / citas** | El diseño de #13 solo listó `adw.ToolbarView.addTopBar`/`setContent` en la tabla de citas, pero el spec pedía también una barra inferior de 24 px; el Apply del fallback (Claude) usó `addBottomBar` — una API real con firma correcta, así que compiló y pasó el gate mecánico — sin que estuviera en la tabla que el PM había verificado. El auditor la encontró comparando el diff contra la tabla línea por línea, pero fue suerte de que la API existiera: es el mismo hueco que dejó pasar dos firmas falsas en #7 (afirmación técnica fuera de la tabla), aplicado esta vez a una llamada real. | La verificación de FASE 5 no debe limitarse a confirmar con `sed` las filas que la tabla del diseño ya trae: hay que releer el diff completo buscando *cualquier* llamada a una API del stack (`adw.`/`gtk.`/`gio.`/`gdk.`) que no aparezca en esa tabla, y verificarla ahí mismo o rechazar el diff. Una tabla de citas que no cubre el 100% de las llamadas del Apply da falsa sensación de haber verificado todo. |
| 2026-08-28 | #13 | **Motor / capacidad** | Reincidencia de la fila de `#4`/`#5` (0 diff / diff parcial tras timeout), con una variante nueva: `core-builder`→aquí `ui-builder` (OpenCode+MiMo) sí escribió un archivo parcial (`app_shell.zig` a medias) antes de agotar los 900 s, pero se quedó atascado re-verificando repetidamente la misma firma (`gobject.ext.as`, `OverlaySplitView`, `HeaderBar`, `ToolbarView`) una y otra vez en vez de avanzar, después de intentar usar `std.process.getEnvVarOwned` — una función que no existe en la stdlib de Zig 0.16 instalada — y no encontrar el reemplazo correcto (`init.environ_map.get`, ya usado en el propio `src/main.zig` para `HERDR_ENV`). Nunca tocó `main.zig`, nunca corrió `zig build`, no entregó tabla de citas. | Un builder que escribe *algo* antes del timeout no es evidencia de que vaya a terminar: la señal de ir a fallback sigue siendo "sin tabla de citas + gate mecánico nunca ejecutado" a los 900 s, no "cero bytes". Además: cuando el diseño necesita una API que no es de GTK/libadwaita/ghostty-vt (aquí, leer una variable de entorno), señalar en el propio diseño el patrón ya usado en el repo (`init.environ_map.get`) en vez de dejar que el builder la redescubra — le habría ahorrado los ~800 s que gastó re-grepeando la stdlib. |
| 2026-08-28 | #8 | **Verificación / timeouts** | El `Watchdog` de `client.zig` dormía `read_timeout_ms` de una sola tirada y solo comprobaba si la petición había terminado *después* de despertar, así que todo `request()` exitoso —el camino normal, no un caso raro— tardaba el timeout completo (medido: 15002 ms con el default de 15 s). Pasó el Apply, pasó QA y pasó una suite verde de 24 tests porque **ningún test medía el camino feliz**, solo el de fallo (`read_timeout_ms` corto, sin respuesta). La señal estaba en el propio `zig build test`: 92 s de reloj contra ~4 s de CPU, y nadie cruzó esa diferencia. Lo encontró el auditor midiendo con su propio reloj, no leyendo el reporte. | Un timeout se verifica en las **dos direcciones**: que corte cuando debe (ya se probaba) y que **no cueste nada cuando no debe** (no se probaba). Cualquier mecanismo de timeout necesita un test de latencia en su camino feliz, no solo uno en su camino de disparo — y un `zig build test`/CI cuyo tiempo de reloj se dispara muy por encima del tiempo de CPU es una pista barata de que algo está durmiendo de más, aunque la suite esté en verde. |
| 2026-08-28 | #8 | **Auditoría / verificación** | `request()` armaba `rpc_err.* = .{ .code = ..., .message = try gpa.dupe(...) }` como un literal único; si `gpa.dupe` fallaba por OOM, `rpc_err.*` quedaba no-`null` con `.message` apuntando a memoria sin inicializar (detalle de Zig, ya documentado en la skill `zig-libghostty`). Pasó el gate mecánico, pasó QA, y pasó **la primera ronda de auditoría** — el propio auditor afirmó en esa ronda que "si `dupe` falla, el literal entero falla", una afirmación sobre semántica del lenguaje escrita de memoria, no ejecutada. Lo encontró la segunda ronda del mismo auditor, barriendo `fail_index` de un `FailingAllocator` sobre cada asignación del camino de error. | Una afirmación sobre semántica del compilador se verifica ejecutándola (aquí, un barrido de `FailingAllocator` por índice de asignación), igual que una firma de API se verifica con `sed` — "razoné que es así" no es evidencia, ni siquiera viniendo del propio auditor. |
| 2026-08-28 | Ola 1 M1 (#8, #13) | **Fleet / lanzamiento** | El orquestador lanzó a los dos hijos con `/kelpie-flow <N>` a secas. Ese comando declara que si lo lanzó un humano (standalone) hay que **detenerse en el scope gate**, así que los dos frenaron en un gate que `/kelpie-fleet` dice que aprueba el orquestador sin pararlos. Peor: cuando el orquestador les mandó instrucciones por el chat del pane, el hijo de #8 no tenía forma de saber que existía un fleet por encima, trató el mensaje como inyección de autoridad —correctamente, según la fila de #4 de esta tabla— y escaló al humano, dejando la ola parada esperando una respuesta que solo él podía dar. | El hijo del fleet **nace sabiendo que es hijo del fleet**: el prompt de arranque lo dice antes del slash-command (quién es el orquestador, que el scope gate y el de diseño los aprueba él, que el gate humano es solo el merge). Un hijo que se cree standalone no está siendo prudente de más: aplica el manual correcto para el rol equivocado, y la defensa antiinyección que eso activa es indistinguible de la deseable. Corolario general: **la procedencia de un canal se establece al arrancar, nunca a mitad de camino** — un mensaje que reclama autoridad después no puede probarla, y no debe poder. Salida sin impersonar al humano: decirle al hijo que descarte el mensaje y verifique el fondo por su cuenta; si el contenido era redundante, tirarlo es gratis. |
| 2026-08-28 | Ola 1 M1 (#8, #13) | **Motor / veredicto** | OpenCode+MiMo falló **los dos Apply de la ola, en paralelo**, con territorios distintos (`src/herdr/` y `src/ui/`), diseños distintos y la corrección de rutas absolutas de la Ola 2 de M0 ya aplicada: en #8 dos intentos con 0 bytes y 0 diff; en #13 escribió uno de los dos archivos que el diseño declaraba y dejó el Apply incompleto. Con las filas de #4 y #5, van **cinco Apply de escritura fallidos consecutivos** del motor externo, y ninguno de los smoke tests de lectura/cita lo predijo. | Cinco fallos con cuatro causas descartadas (permisos, provider, rutas con `~`, timeout) dejan de ser incidentes y son una propiedad del motor: **OpenCode+MiMo no completa un Apply de escritura en este repo**. Deja de tratarse como builder por defecto con fallback, y el fallback pasa a ser el camino. Antes de gastar otra ola en confirmarlo, el PM propone al humano la decisión de arquitectura (retirarlo, o restringirlo a lectura/cita, que es lo único que sí hace en segundos). Regla de método detrás: cuando un mismo componente falla en paralelo, en contextos independientes y con las causas conocidas ya corregidas, el siguiente experimento no es otro intento — es cambiar el componente. |
| 2026-08-28 | Ola 1 M1 (#13) | **Verificación / diseño** | Primer ciclo en que el cruce `git diff --stat` contra la lista de "archivos que se tocan" del diseño (lección de #3) **atrapó algo de verdad**: el Apply de MiMo dejó `src/ui/app_shell.zig` escrito y `src/main.zig` intacto, y el hijo lo detectó solo, descartó el diff y fue a fallback sin que el orquestador interviniera. También destapó su límite: en el mismo ciclo, el Apply usó `adw.ToolbarView.addBottomBar`, una API real pero **ausente de la tabla de citas del diseño**, y el cruce no la vio porque compara archivos, no llamadas. | El cruce de archivos funciona y se queda: es barato y ya pagó. Pero su punto ciego es el mismo que la lección de #7 —una afirmación técnica que entra por fuera de la tabla de citas— un nivel más abajo: **archivos correctos, llamadas nuevas sin citar**. El gate de FASE 5 necesita las dos comprobaciones: diff de archivos contra la lista del diseño, y llamadas nuevas del Apply contra la tabla de citas. Un gate que solo mira el nivel de archivo declara "coincide" sobre un diff que usa APIs que nadie verificó. |
| 2026-08-28 | Ola 1 M1 — **corrección** de las filas `#4`, `#5` y `Ola 1 M1 · Motor / veredicto` | **Diagnóstico / instrumento** | Cinco Apply "fallidos" de OpenCode+MiMo se atribuyeron al motor. Se midió, y el motor **no era la causa**: escribe archivos en 24 s en el repo real y en 54 s dentro de un worktree (misma tarea, worktree ≈2× más lento). Las causas reales eran dos, ambas del arnés: (1) **OpenCode bufferea stdout por bloques cuando no escribe a una TTY** — un run matado a los 15 s deja **42 bytes** de log, así que "0 bytes" nunca significó "0 progreso", significó "el buffer no se vació"; `stdbuf -oL` no lo arregla (no usa stdio de libc), `script -qefc` sí (~11× más log en el mismo tiempo). (2) **`timeout 900` es demasiado corto en este repo**: el log de #13 termina en seco dentro de un `sed` de verificación, sin un error, porque un Apply real cuesta prompt largo + una cita por firma + `zig build` (~75 s) + `zig build test` (~92 s de reloj), todo ×2 en worktree. La regla "0 bytes, 0 diff = fallback ya" que las filas de #4 y #5 dejaron escrita **institucionalizó un artefacto de medición como propiedad del modelo**, y esta ola la aplicó dos veces contra un builder que estaba trabajando. | **Antes de atribuir un fallo a un componente, reproduce el fallo con el componente aislado.** Ninguna de las cinco veces se corrió MiMo a solas con una tarea de escritura: se dedujo del arnés y la deducción entró al ledger como hecho. Reglas concretas que sustituyen a las anteriores: el Apply va **bajo `script -qefc`** y con **timeout ≥2400 s**; el progreso se mide con `git status --short` en el worktree, **nunca en bytes de log**; y un log vacío solo acusa al builder si el árbol también está vacío tras mucho más tiempo del que cuesta el trabajo. Lección de segundo orden y la más cara: **un ledger puede propagar un error de diagnóstico con más autoridad que la que tuvo el error original** — la fila de #5 se escribió citando a la de #4, y a partir de ahí "MiMo no escribe" se leyó como establecido. Una entrada que culpa a un componente necesita decir **cómo se aisló**; si no lo dice, es una hipótesis con formato de conclusión. |
| 2026-08-28 | #14 | **Motor / invocación** | Tres Apply consecutivos de `ui-builder` (OpenCode+MiMo) fallaron por invocación, no por el modelo: (1) `opencode run --agent X "$(cat diseño.md) ..."` compuesto como string plano dentro de `script -qefc "..."` rompe, porque las comillas/paréntesis literales del propio diseño quedan flotando en un texto que un segundo shell (el de `script`) vuelve a parsear — el primer intento terminó en `bash: syntax error`. (2) El flag `-f/--file` de `opencode run` es un array greedy: `opencode run --agent X -f diseño.md 'mensaje'` (o incluso `--file=diseño.md 'mensaje'` después) hace que el `'mensaje'` se trague como si fuera OTRO archivo a adjuntar, error `File not found: mensaje...`. (3) Con la sintaxis correcta (`opencode run --agent X 'mensaje' --file=diseño.md`, mensaje antes del flag), el builder sí adjuntó el diseño pero lo trató como material de referencia a explorar, no como orden — exploró skills/plantillas reales ~7 min y salió con `exit 0` sin escribir nada; el hermano `core-builder` de la misma ola, con el diseño **embebido inline** en el propio prompt de texto (no como adjunto), sí escribió el diff completo en el mismo minuto. | El diseño se pasa **inline en el texto del prompt**, nunca por `-f`/`--file` (esa vía no se trata como instrucción accionable). Para evitar el choque de comillas al embeber texto largo con `$(cat …)`, escribir un script wrapper (`.sh` con el `opencode run --agent X "$(cat diseño.md) ..."` adentro) y pasarle a `script -qefc` la RUTA de ese script, no el comando ya aplanado en un string — así la sustitución de comillas la hace una sola vez el bash que ejecuta el archivo, no un shell que reparsea texto ya aplanado. |
| 2026-08-28 | #14 | **Fleet / lanzamiento** | Al lanzar el Apply con `Bash(run_in_background: true)`, el comando en sí terminaba con `&` de más (doble backgrounding): el shell interno soltaba el proceso de inmediato y la tarea rastreada se reportaba "completed" a los ~15s con el log a medio escribir (solo la cabecera `> ui-builder · mimo-v2.5-pro`), mientras `opencode` seguía corriendo huérfano y sin supervisión. Se detectó porque `git status --short` seguía vacío pese a la notificación de "completado". | `run_in_background: true` ya backgroundea el comando por su cuenta — **nunca** agregar un `&` final al comando que se le pasa; hacerlo produce una tarea que se reporta terminada mucho antes de que el trabajo real termine, y el progreso hay que seguir midiéndolo en el árbol (`git status --short`), nunca en la notificación de la tarea de shell. |
| 2026-08-28 | #9 | **Motor / arnés** | El diseño aprobado (markdown con decenas de spans en backticks) se interpoló verbatim dentro de una cadena de shell entre comillas dobles (`script -qefc "opencode run --agent core-builder \"$PROMPT\""`) — `script -c` pasa esa cadena a una SEGUNDA shell, que sí expande los backticks como sustitución de comandos. El resultado: cientos de "command not found" al final del log, con `COMMAND_EXIT_CODE=127`, que a primera vista se lee como Apply fallido. El Apply en sí había terminado bien antes de esa cola de ruido (confirmado: `zig build`/`zig build test` en verde en el propio log, y `git status` con los archivos reales creados). | Nunca interpolar un documento con backticks/comillas dentro de una cadena de shell que se re-parsea en otra shell (`script -c`, `bash -c`, `ssh host "..."`). Pasar el prompt largo por un **script wrapper**: escribir un `.sh` que contenga el `opencode run --agent X "$(cat diseño.md) …"` y darle a `script -qefc` la RUTA del script, nunca el comando ya aplanado en un string. (Corregido por el orquestador al consolidar la ola: la redacción original de esta fila recomendaba `--file`, pero el ciclo de #14 demostró en la misma ola que esa vía **no** funciona — el builder trata el adjunto como material de consulta, no como orden, y sale con `exit 0` sin escribir. Ver la fila `#14 · Motor / invocación`.) Y antes de leer un `COMMAND_EXIT_CODE` no-cero como veredicto, revisar si el log tiene señales de éxito (gate mecánico verde) ANTES del punto donde aparece el error — un 127 al final de un log largo puede ser ruido posterior, no el resultado del comando principal. |
| 2026-08-28 | #9 | **Motor / destructivo** | Durante un fix de ronda 2, `core-builder` (OpenCode+MiMo) ejecutó `git stash` (bare, sin `-u` ni mensaje) por su cuenta para comparar el árbol limpio contra su cambio en curso, y terminó el turno sin hacer `stash pop` — dejó su propio fix en el stash stack compartido del worktree, sin avisar. El PM lo recuperó a tiempo porque capturó el SHA del stash apenas lo vio (`git stash list --format='%H %gd %gs'`) y lo restauró con `git stash apply <sha>` (nunca `pop`), verificó el contenido, y lo dropeó explícitamente tras commitearlo. | Un builder externo puede ejecutar git de forma destructiva sin que la orden se lo pida — la regla del arnés ("nunca `stash`/`stash pop` a ciegas, captura el SHA, usa `apply`") aplica igual cuando quien lo dispara es un subproceso del builder, no solo el propio PM. Después de cada Apply/fix, `git stash list` es parte de la verificación de FASE 5 tan barata como `git status --short` — un `stash` fantasma dejado por el builder es indistinguible de "no pasó nada" si no se mira. |
| 2026-08-30 | Ola 2 M1 (#9, #14) — **consolidación del orquestador** | **Fleet / procedencia** | El prompt de arranque de los dos hijos decía «el scope gate y el gate de diseño **los aprueba el orquestador** por este mismo chat». Cada hijo leyó «orquestador» como su propio rol —corre `/kelpie-flow`, luego es el PM— firmó su diseño con `> Aprobado por: orquestador PM` y siguió de largo sin preguntar. Los dos, por separado, en la misma ola. Los diseños resultaron buenos y el orquestador los aprobó por revisión posterior, pero el gate no existió: se enteró de lo aprobado después de que el Apply ya corría. | La corrección de la Ola 1 (el hijo nace sabiendo que es hijo) era necesaria pero no suficiente: **la procedencia tiene que nombrar al pane, no al rol**. El arranque dice «el gate lo aprueba `wA:p2`; escríbeme por el chat y **espera respuesta**», no «lo aprueba el orquestador». Un rol es ambiguo cuando el hijo también ocupa un rol con ese nombre; un `pane_id` no lo es. Regla general: en un sistema donde varios agentes comparten vocabulario de roles, **la autoridad se direcciona por identidad de canal, nunca por nombre de función**. |
| 2026-08-30 | Ola 2 M1 (#9, #14) — **consolidación del orquestador** | **Fleet / hotspots** | El plan de olas leaseó los hotspots de código (`build.zig`, `src/main.zig`, `src/ui/app_shell.zig`) y funcionó: ni un choque en código pese a que los dos hijos tocaron `src/ui/`. El conflicto real del rebase vino de donde nadie miró: **`CONCERNS.md` y `lessons-learned.md`**, que los dos hijos escriben al cerrar su ciclo por diseño del flow. El segundo PR chocó en ambos. | Los ledgers son hotspots del enjambre igual que `build.zig`, y la tabla de territorios de `CLAUDE.md` no los menciona porque solo habla de código. El plan de olas los declara siempre, con su regla de resolución escrita **antes** del conflicto: son **append-only**, así que se conservan **las dos entradas**, jamás se elige una. Corolario: cada hijo escribe su entrada al final y en bloque propio, para que el conflicto sea trivial de resolver. |
| 2026-08-30 | Ola 2 M1 (#9, #14) — **consolidación del orquestador** | **Ledger / contradicción** | Los dos hijos escribieron una lección sobre cómo pasarle el diseño al builder, y **se contradicen**: #9 concluyó «pasar el prompt largo como archivo adjunto con `--file`» y #14 demostró en la misma ola que `--file` **no** funciona (el builder trata el adjunto como material de consulta, explora 7 min y sale con `exit 0` sin escribir). Cada uno razonó bien desde lo que vio; ninguno podía ver al otro. De no corregirse, el ledger habría entrado a la próxima ola recomendando exactamente la vía que acababa de fallar. | Cuando dos hijos escriben sobre el mismo tema, **el orquestador no consolida concatenando: cruza las filas entre sí antes de commitear**, y donde chocan resuelve con la evidencia más fuerte (aquí: la observación empírica de #14 gana sobre la inferencia de #9) dejando anotado que la fila fue corregida al consolidar y por qué. Un ledger que se contradice a sí mismo es peor que uno incompleto: las dos filas se citan con la misma autoridad y la siguiente ola elige al azar. Esto solo se ve desde arriba — es, literalmente, el trabajo que justifica que haya un orquestador. |
| 2026-08-30 | Ola 2 M1 (#9) — **consolidación del orquestador** | **Vigilancia / instrumento** | El monitor del orquestador vigilaba `apply-9.log` y cantó `ESTANCADO` dos veces mientras el hijo trabajaba: las rondas de fix escriben en `apply-9-round2.log` y `apply-9-round2b.log`, que el glob no cubría. Peor: el hijo dedujo de un estado intermedio que tenía «2 errores de compilación», lanzó una ronda 2b que corrió 17 min sin tocar un archivo, y estaba a punto de gastar el fallback a Claude — cuando `zig build test` sobre su HEAD ya pasaba en verde. Lo desmontó el orquestador ejecutando el gate él mismo en el worktree del hijo. | Dos reglas. (1) **Un monitor que mide progreso debe cubrir todas las rondas** (`apply-<N>*.log`), y su señal fuerte es `git status` + commits, no bytes de log — un instrumento que solo ve la ronda 1 acusa de estancamiento a un builder que trabaja. (2) Antes de autorizar un fallback por «el builder no arregla los errores», **el orquestador corre el gate mecánico él mismo sobre el HEAD del hijo**: la premisa del hijo puede venir de un estado intermedio que ya no existe. Un fallback gastado por una premisa falsa cuesta una ola entera y ensucia el ledger con un fallo del motor que nunca ocurrió. |
| 2026-08-30 | #17 | **Verificación / contrato oculto** | El diseño de #17 puso `.handles_command_line = true` en el flag de `adw.Application.new`, pero `gio.Application.run(app, argc, argv)` — línea preexistente, sin tocar en el diff — seguía llamándose con `argc=0, argv=null`. Con ese flag, el propio binding documenta que argv real es indispensable para que GIO reenvíe los argumentos a la instancia primaria; sin él, el despacho entero (`focus`/`reload-theme`/`reload-font`) era inerte pese a 61/61 tests en verde, porque los tests solo cubrían el parser puro, nunca el cableado GIO↔parser. Lo cazó el auditor ejecutando el binario de verdad (`dbus-run-session`), no leyendo el código. | Cuando un Apply cambia un flag/modo/enum que altera el contrato de un objeto (aquí: `ApplicationFlags`), **entran a la verificación también las llamadas ya existentes sobre ese objeto**, no solo las líneas nuevas del diff — un `git diff` no muestra el código cuyo significado cambió sin cambiar de texto. Corolario: `zig build test` en verde no es evidencia de que el despacho funcione si ningún test ejercita el pegamento; un gate de ejecución real (aunque sea vía `dbus-run-session`, no la sesión Wayland completa) barre esta clase de bug en segundos donde ningún test unitario lo ve. |
| 2026-08-30 | #17 | **Verificación / hueco declarado falso** | El diseño de #17 declaró como "hueco no documentado" que el binding no explica qué hace GIO con el `c_int` que retorna el handler `command-line`, y por eso el Apply puso `return 0;` fijo — que pisaba en silencio cualquier `setExitStatus(cmdline, 1)` llamado dentro del handler. La doc SÍ estaba: `gio2.zig:2026-2029`, cuatro líneas por encima de la firma de `setExitStatus` que el propio diseño ya citaba, dice literalmente que GIO pasa el retorno del handler a `setExitStatus` después de que retorna. El bug sobrevivió un Apply completo y la primera ronda de auditoría (que aprobó implícitamente esa parte); lo cazó la **segunda** auditoría, con un experimento controlado (`return 0` → `return 7`, el exit code observado cambió). | Un "hueco declarado" en un diseño es una afirmación sobre la fuente, igual que una cita — y se verifica de la misma forma, con `sed -n` mirando alrededor de la línea ya citada, no dándolo por cierto porque "no lo dice" suena razonable. Regla operativa: antes de aceptar un hueco declarado en un diseño, releer unas 10-15 líneas de contexto alrededor de cada cita de esa misma API — la respuesta muchas veces está a un scroll de distancia de la firma que sí se verificó. |
| 2026-08-30 | #11 | **Verificación / invariantes** | El auditor denegó una regresión aunque el fix de producción (timeout en `readHerdrStatus`) ya era correcto: el test de guardia que el PM escribió medía `elapsed < 5s` contra un fake que cuelga solo 4s, así que pasaba en verde con y sin el `.timeout`. Corrido a mano en ambas versiones del archivo: 4007ms sin el fix, 3004ms con — el margen del assert (5s) caía fuera de los dos, no entre ellos. | Reincidencia un nivel más abajo de la fila `#5 · Verificación / invariantes` ("un invariante que solo vive en un comentario no sobrevive"): aquí el invariante ya vivía en un test y el test tampoco lo protegía, porque nadie lo corrió sin el fix. Un test que se escribe para probar un timeout/fix no está verificado hasta que se confirma que falla en rojo SIN el cambio — no solo que pasa en verde con él. El margen del assert debe caer estrictamente entre el tiempo "con fix" y el tiempo "sin fix", nunca fuera de ambos. |
| 2026-08-30 | Ola 3 M1 (#10, #11) — **consolidación del orquestador** | **Motor / forma y posición de la orden** | **Los dos** hijos fallaron su Apply de ronda 1 con `exit 0` y 0 diff, con prompts de cierre débiles. La diferencia estuvo en la ronda 2: #11 puso la orden **al final** del prompt y MiMo escribió 866 líneas de una sentada; #10 puso la misma clase de orden **al principio**, enterrada bajo el diseño entero, volvió a salir `exit 0` y se gastó un fallback. Diagnóstico del propio hijo de #11, más preciso que el del orquestador desde fuera: **MiMo no confía en una tabla de citas que se le entrega ya hecha y la re-deriva desde cero** salvo que se le dé permiso explícito de no hacerlo — eso consumió la ronda 1 de ambos y las dos de #10. | El prompt de Apply **termina** con la orden, no empieza con ella. Plantilla obligatoria (wrapper de #11): diseño primero; al final un bloque imperativo con (a) permiso explícito de NO re-verificar («la tabla de arriba ya está verificada, úsala tal cual, no investigues más»), (b) los archivos exactos permitidos y los prohibidos, (c) «impleméntala literal, no la rediseñes», (d) el comando de cierre como un solo `&&` imperativo, no como prosa. **Corrige el alcance de las filas `#4`, `#5` y `Ola 1 M1 · Motor / veredicto`:** la variable no era el motor, ni el buffering, ni el timeout — es la forma y la posición de la orden. |
| 2026-08-30 | Ola 3 M1 (#10, #11) — **consolidación del orquestador** | **Motor / escalón antes del fallback** | #11 se recuperó de su `exit 0` sin diff **relanzando al mismo builder** con el prompt reforzado, ~8 min. #10 fue a `core-builder-fallback` (Claude) tras una ronda 2 mal posicionada: el fallback se gastó por un defecto de la instrucción del orquestador, no por incapacidad del motor. | Un `exit 0` sin diff **no va directo al fallback** cuando el motivo visible es «solo investigó, nunca escribió»: se relanza al mismo builder con el prompt reforzado de la fila anterior, que es mucho más barato. El fallback se reserva para timeout muerto, cita falsa, o un segundo `exit 0` con el prompt **ya reforzado y bien posicionado**. |
| 2026-08-30 | Ola 3 M1 — **consolidación del orquestador** | **Ledger / la corrección no desactiva a la fila corregida** | La fila 55 corrigió a las filas `#4` y `#5` («0 bytes, 0 diff = fallback ya» era un artefacto de medición del arnés). Aun así, el hijo de #10 citó **las filas `#4`/`#5`** para justificar su fallback: están antes en la tabla y se leen con la misma autoridad que la corrección que las invalida. Es la segunda ola seguida en que el ledger propaga un diagnóstico ya refutado. | Una corrección debe marcar la fila corregida **en su propio texto** como superada (`⚠️ SUPERADA por …`), no solo refutarla quince filas más abajo. Aplicado en este mismo commit a las filas `#4` y `#5`. Un lector —humano o agente— lee de arriba abajo y se detiene en la primera fila que parece responder su pregunta. |
| 2026-08-30 | Ola 3 M1 — **consolidación del orquestador** | **Vigilancia / la señal cruda no distingue quién espera a quién** | Tres versiones del `Monitor` en una sola ola. `agent_status: idle` significa tanto «te espero a ti» como «espero a mi propio Apply/subagente», y vigilar sobre esa señal cruda hizo que el orquestador gastara turnos en eventos que no requerían nada, mientras el latido de un hijo ocupado se veía idéntico al de uno bloqueado. | La señal útil no es el estado del pane: es ese estado **cruzado con** (a) si hay un proceso `apply-*`/`fallback-*` vivo para ese worktree y (b) el árbol (`git status --short` y commits por delante de `develop`). Todo lo que tenga proceso vivo es silencio, igual que `working`. El evento debe traer ya el veredicto (`TE-ESPERA`) y los datos para decidir, no obligar a ir a buscarlos. |
| 2026-08-30 | Ola 3 M1 (#10) — **consolidación del orquestador** | **Canal / un subagente puede terminar sin entregar** | `auditor-10` cerró **dos veces** con `idle_notification` y sin reporte; `TaskOutput` no lo encontró ni por nombre, ni por `agent_id@session`, ni por el `[ref]` de `ListAgents`. El hijo se quedó esperando en bucle a un agente que ya no existía, y sin veredicto no podía ni aprobar ni denegar. | Todo subagente cuyo resultado sea **un gate** escribe su veredicto en un **archivo del worktree** antes de terminar (`audit-<N>.md`, veredicto binario en la primera línea); el mensaje es cortesía, el archivo es el contrato. Es el mismo principio que ya rige la medición de progreso: **no midas por el canal que puede fallar, mide por el artefacto que queda en el árbol.** Y un hijo que espera a un subagente ya terminado debe leer el resultado de la tarea, no esperar un mensaje que no va a llegar. |
| 2026-08-30 | Ola 3 M1 (#10, #11) — **consolidación del orquestador** | **Procedencia / el gate de diseño se autoaprueba** | Los **dos** hijos firmaron su diseño como «Aprobado por: orquestador PM» y lo commitearon **sin pasárselo al orquestador**. Ninguno mintió: el prefijo de arranque del fleet establece de quién reciben órdenes, pero no les dice que deben **detenerse**. Dos de dos es un defecto del prompt del orquestador, no de los hijos. | El prefijo de arranque del fleet debe decir literalmente: «tras redactar el diseño, **PARA** y espera aprobación por este chat antes de tocar código». Establecer la autoridad no implica establecer el punto de parada; hay que decir ambas cosas. |
| 2026-08-30 | Ola 3 M1 (#10) — **consolidación del orquestador** | **Verificación / el instrumento del PM también falla** | El orquestador verificó la tabla de citas con `sed -n 'Ap;Bp'` y leyó la salida **en el orden de sus argumentos**; `sed` imprime en **orden de archivo**. Resultado: acusó de transposición a una tabla correcta y ordenó «corregirla». El hijo **se negó a editar contra su propia lectura verificada** y pidió cruzar la fuente antes de tocar nada — tenía razón, y eso evitó un commit malo. | Dos reglas. (1) El PM verifica citas con **una invocación por línea** (`sed -n 'Np'`), nunca varias en un solo comando cuyo orden de salida no controla. (2) Un hijo **debe disputar** una corrección del PM que contradiga su propia lectura verificada, y el PM debe tratar esa disputa como señal, no como desobediencia: la autoridad del orquestador no es autoridad sobre los hechos del toolchain. |
| 2026-08-30 | Ola 3 M1 (#10, #11, #17) — **consolidación del orquestador** | **Territorio / el lease funciona, el ledger no se puede leasear** | Con tres ramas vivas simultáneas, el reparto de FASE 0 (`src/main.zig` arrendado a #17; el apéndice de `build.zig` repartido entre #10 y #11) produjo **cero conflictos de código**. El **único** conflicto de toda la ola fue `lessons-learned.md`, donde escriben todos por diseño. | El reparto de territorio no cubre los ledgers compartidos, y no hay forma de que lo cubra. El conflicto ahí se resuelve **siempre conservando ambos lados** (son append-only) y el orquestador debe **decirlo explícito** al ordenar el rebase: «quedarse con la mía» es la resolución intuitiva y pierde en silencio la lección de la otra rama. Lo mismo aplica al apéndice de `build.zig`: se conservan los dos bloques, ninguno se descarta. |
| 2026-08-30 | Ola 3 M1 (#11) | **QA / un test que no discrimina** | El auditor de #11 denegó una regresión **aunque el fix de producción era correcto**: el test de guardia ponía el umbral en < 5 s contra un fake que colgaba 4 s, así que **pasaba con y sin el fix**. El propio auditor bajó el umbral a 3,5 s y verificó que fallaba en rojo sin el fix y en verde con él. | Un test que pasa con y sin el arreglo **no prueba el arreglo**, y un CI verde no lo delata. Correr el test en **las dos direcciones** (con y sin el fix) antes de darlo por bueno es trabajo de QA, no una iniciativa afortunada del auditor. |

| 2026-08-30 | Ola 2B M1 (#17) — **consolidación del orquestador** | **Diseño / huecos declarados** | El diseño de #17 citó `command_line.connect` con su línea exacta y la firma correcta, y en la misma tabla declaró un hueco: «el binding no documenta el significado del `c_int` que retorna el signal; el diseño no depende de él (usa `setExitStatus` para todo)». El orquestador verificó las 9 citas con `sed -n` una por una —todas exactas— y aprobó. La doc **sí existía**, cuatro líneas por encima de la firma de `setExitStatus` que el propio diseño citaba: *«The return value of the command-line signal is passed to this function when the handler returns»*. Es decir, GIO pisa cualquier `setExitStatus` con el retorno del handler. El `return 0` fijo —comentado en el código como «inocuo» sobre la base de ese hueco— anulaba en silencio el exit 1 de todos los caminos de error y rompía el criterio 3 del issue. Costó una tercera ronda de Apply y una escalada al humano. | **Un hueco declarado no es una zona neutral: es una afirmación sobre la fuente, y se verifica con el mismo `sed -n` que las citas.** «La fuente no documenta X» tiene exactamente el mismo estatus epistémico que «la firma es Y» —ambas son claims sobre el mirror— pero el gate de diseño solo audita las segundas, porque la tabla de citas solo tiene filas para lo que sí se encontró. Reglas: (1) todo hueco declarado lleva su propio `sed -n` de respaldo, mostrando el rango leído donde la respuesta debería estar y no está; (2) sospechar especialmente del hueco que dice «no depende de esto» sobre un valor que el runtime sí usa —esa frase es la que apaga la búsqueda—; (3) leer el contexto de una cita, no solo su línea: aquí la respuesta estaba a cuatro líneas de una firma ya citada, dentro del mismo doc-comment. El fallo no fue del builder ni del hijo: el orquestador verificó nueve afirmaciones positivas y ni una negativa. |
| 2026-08-31 | #12 | **Verificación / gate mecánico que no cubre el camino** | `zig build test` pasó en verde en la ronda 1 del Apply con dos bugs de memoria reales adentro (double free en `WorkspaceInfo`/`TabInfo`/campos opcionales de `Agent` apuntando a un `json.Parsed` ya liberado) — el PM los cazó releyendo el diff, no el gate. Corregidos replicando el patrón ya correcto de `Agent` (`gpa.dupe`/`gpa.free`), pasó auditoría… y la primera ronda de auditoría **denegó** por un tercer bug que ni el gate ni la relectura del PM habían visto: `fetchPut` de `std.hash_map` (Zig 0.16, verificado en `hash_map.zig:901-912`) **conserva la key existente** cuando ya había una entrada — solo pisa el value — así que liberar `old.key` (el patrón intuitivo, «lo que fetchPut devuelve es lo viejo, se libera») es un double-free en cuanto se toque esa entrada otra vez. Pasó el gate mecánico las dos veces porque **ningún test tocaba `workspace_created/updated/tab_created`** — el escenario "sin fugas" del diseño solo ejercitaba `workspace_renamed`/`tab_focused`, los dos únicos handlers que no usan `fetchPut`. | Dos reglas. (1) **Replicar un patrón de memoria correcto a un tipo no basta si se replica de memoria**: `Agent` tenía el dupe/free bien desde la ronda 1, pero `WorkspaceInfo`/`TabInfo` y el helper de campos opcionales de `pane_agent_status_changed` no lo heredaron automáticamente — cada tipo/handler que persiste un `[]const u8` de un `json.Parsed` necesita su propio par dupe-al-guardar/free-al-reemplazar verificado, no solo "está bien en el archivo hermano". (2) **Un método de `std` que reemplaza una entrada existente (`fetchPut`, y por extensión cualquier `getOrPut`) no se asume simétrico** — verificar en la fuente (aquí `hash_map.zig`) qué conserva y qué pisa antes de decidir qué liberar; el nombre "fetch-put-devuelve-lo-viejo" invita a asumir que TODO lo viejo (key incluida) quedó fuera del mapa, y aquí la key no. (3) Corolario de proceso: `zig build test` en verde **no es evidencia de un handler sin bugs si ningún test lo ejercita** — antes de aprobar, cruzar qué `EventKind` tiene test propio contra la lista completa que el diseño declara manejar, no solo correr la suite. |
| 2026-08-31 | #15 | **Apply / dos clases distintas de "exit 0 sin diff"** | Ronda 1 de Apply de #15 salió con exit 0 y sin diff porque el propio wrapper del PM anidaba `script -qefc "opencode run ... \"${PROMPT}\""` **dentro** del `.sh`: `script` re-ejecuta ese string completo en un segundo shell, y los backticks de markdown del diseño (` `` `código` `` `) se reinterpretaron ahí como sustitución de comandos, corrompiendo el prompt en basura (`bash: line 1: .moved_out: command not found`, etc.) — un bug del PM, no del builder. Corregido (el `.sh` llama a `opencode` directo; `script -qefc` envuelve la invocación del `.sh`, no al revés), las rondas 2 y 3 con el prompt ya limpio **también** salieron con exit 0 y sin diff, pero por una causa distinta: el proceso `opencode`/MiMo terminó en silencio a mitad de un `Read`, sin error visible, muy por debajo del presupuesto de tiempo (6-7 min de 2400s) — ronda 2 tras agotar el turno re-verificando la tabla de citas completa pese a permiso explícito de no hacerlo, ronda 3 cortada aún antes. Ninguna reinforzación de prompt lo arregló. | **"Exit 0 sin diff" no es una causa, son (al menos) dos, y cada una pide un diagnóstico distinto antes de reintentar o hacer fallback.** (1) Si el log muestra basura de shell (`command not found` sobre fragmentos de la propia spec) — es corrupción del wrapper del PM: revisar el propio `script -qefc` por anidamiento de un string con backticks, no relanzar al builder todavía. (2) Si el log es limpio pero se corta a mitad de una herramienta muy por debajo del timeout — es el motor externo cayendo en silencio, no un problema de prompt: reforzar el prompt una vez (regla ya escrita en `CLAUDE.md`), y si se repite, es la segunda causa de fallback legítima aunque el `git diff` nunca haya sido "insatisfactorio" — simplemente no existe. |
| 2026-08-31 | #15 | **Auditoría / una regla que ya falló tres veces necesita evidencia adjunta, no confianza** | El auditor DENEGÓ #15 dos veces seguidas por la misma clase de bug del ledger (`lessons-learned.md` #5, #11, Ola 3 M1: «un test que pasa con y sin el arreglo no prueba el arreglo»), la tercera reincidencia documentada de esa regla exacta — primero en el test del criterio 1 (concern 3, corregido en la ronda de fallback), luego en el test del criterio 4 (bloqueante 2 de la 2ª pasada: el catch-up reload satisfacía el `expect(reload_count >= 1)` él solo, sin que el re-arme real se llegara a probar). El humano autorizó una 3ª iteración (fuera del límite normal de 2) con una condición explícita: el PM debía correr el experimento en las dos direcciones **él mismo** y pegar los dos resultados en el reporte, no solo aplicar el fix del auditor y confiar en que funcionaba — "no basta con que el auditor lo dijera". El auditor de la 3ª pasada repitió el experimento de todas formas, de forma independiente, y solo entonces aprobó. | **Cuando una regla del ledger reincide por tercera vez en el mismo issue, el siguiente fix no se manda a auditar con la promesa de que funciona: se manda con las dos corridas (roja sin el mecanismo, verde con él) ya adjuntas en el propio mensaje al auditor.** No reemplaza la verificación independiente del auditor —la 3ª pasada la hizo de nuevo por su cuenta y fue lo que realmente aprobó— pero acorta la vuelta: el auditor gasta su turno confirmando en vez de descubriendo desde cero, y una reincidencia de tercer orden dispara evidencia adjunta como default, no como pedido especial del humano. |
| 2026-08-31 | #15 vs #12 (rebase) | **Territorio / los ledgers vuelven a chocar, como ya predijo el ledger** | `feature/15-theme-watcher` quedó detrás de `develop` porque #12 mergeó (`64617d5`) mientras #15 seguía en auditoría. `git rebase origin/develop` marcó conflicto en `CONCERNS.md` y `lessons-learned.md` — exactamente el escenario que la fila de Ola 3 M1 (línea 74 de este mismo archivo) ya documentó: «el reparto de territorio no cubre los ledgers compartidos». El orquestador lo avisó explícitamente antes de que el PM llegara al rebase, en vez de que el PM lo descubriera solo al chocar. | La entrada de Ola 3 M1 seguía siendo válida sin cambios — esto no es una regla nueva, es la primera vez que un orquestador de fleet **la aplicó preventivamente** en vez de dejar que el PM la redescubriera. Corolario de proceso: cuando el orquestador ya sabe que dos ramas del fleet van a chocar en un ledger append-only, avisar el conflicto (y la resolución: conservar ambos lados) **antes** del rebase, en la misma instrucción que autoriza seguir — no es información que valga la pena que el PM redescubra por las malas. |
| 2026-08-31 | Ola B M1 (#12, #15) — **consolidación del orquestador** | **Vigilancia / cinco instrumentos y el que sirve es el tonto** | Quinta iteración del `Monitor` en dos olas, y cada intento de filtrar ruido introdujo una forma nueva de **perder la señal**: (a) `agent_status` crudo no distingue «te espero» de «espero a mi subagente»; (b) suprimir por proceso vivo con `pgrep -f "ui-builder-fallback"` dio dos falsos positivos y **ocultó 15 minutos a un hijo bloqueado** esperando una decisión mía; (c) contar archivos con `git status \| grep -v` devolvió **cero** durante media hora porque `grep` estaba roto en ese entorno (`unknown option '-G'`), y reporté al humano «#15 no ha escrito nada» cuando tenía 16 KB de código y una auditoría encima. | Un vigilante de orquestador **no filtra por listo, filtra por barato**: todo lo que no sea `working` se emite, y el evento trae los datos para decidir (archivos, commits, `detras=`). Corolario que generaliza la fila 55 un nivel más arriba: **el instrumento que mide el árbol también puede fallar**, y un `git status` filtrado por una herramienta rota devuelve vacío — indistinguible de «no hizo nada». Si un dato acusa a un hijo de no trabajar, **reprodúcelo con otro comando antes de reportarlo**. |
| 2026-08-31 | Ola B M1 (#12) — **consolidación del orquestador** | **Gate de diseño / el recorte YAGNI necesita cruzarse contra el CÓDIGO, no solo contra el issue** | El diseño de #12 recortaba `pane_focused` por YAGNI, con el argumento correcto de que el Store ordena por urgencia y no gestiona layout — y el issue tampoco lo lista. Pero `Events.zig:75` **ya se suscribe** a `pane.focused`: el evento llega, `applyEvent` lo habría tirado con `else => {}`, y el campo `Agent.focused` —que el issue **sí** exige— habría quedado rancio, con el criterio de #18 («un `done` del pane que estoy mirando no notifica») fallando en silencio. Costo de no recortarlo: cinco líneas del mismo patrón que ya iba a escribir. | Un recorte YAGNI en el gate de diseño se valida contra **tres** fuentes, no una: el alcance del issue, **el código ya mergeado que alimenta al módulo**, y los criterios de aceptación de los issues que lo consumen. El corte de #12 era coherente con el issue y aun así abría un agujero, porque la dependencia estaba en una rama que aterrizó **dos olas antes**. Y la regla barata detrás: si el recorte deja vivo un CAMPO que nadie actualiza, no es un recorte — es una trampa. |
| 2026-08-31 | Ola B M1 (#12, #15) — **consolidación del orquestador** | **Motor / la plantilla de prompt se confirma, y el PM sigue siendo una causa de fallo** | Primera ola con la plantilla de Apply de la Ola 3 aplicada desde el arranque (orden imperativa **al final**, permiso explícito de no re-verificar citas, archivos permitidos, comando de cierre en un solo `&&`). #12 escribió 38 KB de `Store.zig` en la primera ronda útil. #15 gastó dos rondas sin diff, y **una de las dos fue un bug de quoting del propio PM**, no del motor — lo reportó el hijo en el cuerpo de su PR, sin que nadie se lo pidiera. | La plantilla funciona y se queda. Pero el veredicto sobre el motor externo sigue contaminado por el arnés: antes de anotar «MiMo falló» hay que descartar que el wrapper esté roto — es la tercera vez en el ledger (filas `#4`/`#5` superadas, fila 58 del quoting con backticks, y ahora esta). **Un fallo de Apply se atribuye al motor sólo tras leer el wrapper generado**, no el log. Y que el hijo declare el error del PM en su propio PR es la conducta a reforzar: el ledger sólo sirve si las filas que acusan al PM las escribe alguien. |
| 2026-08-31 | Ola B M1 (#12, #15) — **consolidación del orquestador** | **Auditoría / tres de tres olas encuentran defectos que el CI verde no ve** | #10: use-after-free en el dispatcher. #11: colgado contra servidor mudo + test que no discriminaba. #12: **tres** bugs de memoria (double-free vía `fetchPut`, `pane_focused` mutando agentes ajenos, `free` antes del `dupe`). #15: el `.renamed` real trae el nombre **viejo** en `file` (el watcher no habría recargado nunca) + otro test que pasaba con y sin el fix. **Cinco issues, cinco auditorías con hallazgos reales, cero falsos positivos**, y en los cinco el CI estaba verde antes de la auditoría. | El auditor Opus no es un trámite ni un lujo: es el único gate que ha encontrado defectos en **todos** los ciclos, y ninguno era detectable por compilación ni por tests en verde. La regla «el auditor nunca se abarata» del CLAUDE.md queda respaldada por datos, no por principio. Nota de segundo orden: **tres** de esos hallazgos fueron «un test que pasa con y sin el arreglo» (#11, #15, y la ronda 2 de #15) — correr los tests en las dos direcciones debe ser trabajo por defecto de QA, no un hallazgo afortunado del auditor. |
| 2026-08-31 | Ola B M1 — **cierre del orquestador** | **Cierre / la basura invisible que no está en `git worktree list`** | Al limpiar la Ola B aparecieron **12 ramas remotas huérfanas** de PRs mergeados, acumuladas desde el spike B (#62) a lo largo de cuatro olas. Causa: `gh pr merge --delete-branch` falla cuando un worktree tiene la rama tomada, y **su error habla solo de la rama local** (`cannot delete branch ... used by worktree`) — se lee como si el borrado remoto sí hubiera ocurrido. La verificación de cierre de FASE 4 comprobaba `git worktree list` e issues abiertos, nunca `git branch -r`, que es exactamente donde se acumulaba. | Orden obligatorio en el cierre: **worktree fuera → rama local → `git push origin --delete` explícito**, aunque ya hayas usado `--delete-branch` (si falló, este limpia; si no, falla sin daño). Y la verificación de cierre son **cinco** comprobaciones, no dos: `git worktree list`, `git branch`, **`git branch -r`**, `gh pr list --state open`, `gh issue list --state open`. Aplicado a `.claude/commands/kelpie-fleet.md` (FASE 4) y `kelpie-flow.md` (FASE 8) en este mismo PR. |
| 2026-08-31 | Ola B M1 — **cierre del orquestador** | **Verificación / con squash, el grafo de commits miente sobre qué está mergeado** | Al ir a borrar esas 12 ramas, el primer instrumento dio la respuesta **contraria** a la verdad: `git rev-list origin/develop..<rama>` reportaba entre 1 y 8 commits «sin integrar» en **todas**, incluidas las mergeadas hacía minutos. Es inherente al squash merge: el merge crea un commit nuevo en `develop` y los commits originales de la rama nunca son ancestros suyos. Fiarse de eso lleva a no borrar nunca nada — o peor, a creer que hay trabajo que rescatar donde no lo hay. | En un repo que mergea con squash, **«¿está esta rama mergeada?» se le pregunta al PR, nunca al grafo**: `gh pr list --state all --json headRefName,state`. `git branch --merged` y `git rev-list base..rama` no son válidos aquí. Regla general detrás, tercera aparición en este ledger: **cuando un instrumento acusa (de no trabajar, de no estar mergeado), reprodúcelo con otro instrumento antes de actuar** — las otras dos veces fueron los bytes de log (fila 55) y el `git status \| grep` roto (fila de Vigilancia de esta misma ola). |
| 2026-08-31 | #16 (Ola C M1) | **Ledger / se escribe y no se hace cumplir** | El auditor cruzó el diff contra este archivo y encontró **cinco filas vigentes reincididas en un solo issue**: contrato oculto en líneas preexistentes (`#17`), test que no discrimina (cuarta vez: `#11`, `#15`×2, `Ola 3 M1`), literal de struct con campos falibles (cuarta vez: `#5`, `#8`, `request()`), APIs usadas fuera de la tabla de citas (`#7`, `#13`), y gate mecánico verde sobre un camino que ningún test ejercita (`#12`). **Ninguno de los tres bloqueantes era un hallazgo nuevo del enjambre**: los tres tenían su regla ya escrita. El ledger se lee en FASE 1 como quien lee un prólogo, y solo se *aplica* en FASE 7, que es el último gate antes del PR — o sea, cuando corregir ya cuesta una ronda de builder. | **La lectura del ledger en FASE 1 no vale como cumplimiento: hay que convertirla en obligaciones del diseño.** Al escribir el diseño, el PM toma cada fila vigente que toque el territorio, el motor o la forma del cambio, y la baja a algo verificable *en ese issue*: una firma más en la tabla de citas, un escenario Gherkin, una línea preexistente que el diff no toca pero que fija el contrato, un sabotaje concreto que QA debe ejecutar. Una fila que el diseño no convierte en obligación es una fila que se va a reincidir y a descubrir en auditoría. |
| 2026-08-31 | #16 (Ola C M1) | **Gate / lo que solo se ve ejecutando** | Dos de los tres bloqueantes eran **invisibles para toda la suite y evidentes al abrir la ventana**: el sidebar pintaba el modelo viejo (GTK bindea síncronamente dentro de `gio.ListStore.splice` y el swap de `rows` iba después), y `--demo-sidebar` **nunca abrió una ventana** —salía con `unknown subcommand`— así que los escenarios Gherkin 3, 5 y 6 no se habían podido ejecutar jamás. `zig build test` estuvo verde todo el tiempo. Ni el builder, ni QA, ni el PM corrieron el binario en toda la ronda; solo el auditor, por iniciativa propia. QA además **midió** que un `Sidebar` real segfaultea headless, o sea que ese código es inalcanzable por unit test por construcción. | **Todo issue que produce un widget cierra con un gate de ejecución real, y lo corre el PM, no el auditor.** Compilar y pasar tests no es haber visto la cosa funcionar. El gate mínimo es: lanzar el binario con su flag de demo bajo `dbus-run-session`, comprobar que **sigue vivo** (un `timeout` que devuelve 124, no un exit 0), capturar con `grim` y **mirar la captura**. Y todo flag nuevo se prueba con su control negativo: que `--demo-sidebar` funcione no dice nada si nadie comprueba que `bogus` sigue fallando. Coste medido: minutos. Lo que destapó: los dos bloqueantes graves y, de regalo, la prueba visual de que el arreglo del primero funciona. |
| 2026-08-31 | #16 (Ola C M1) | **Verificación / el instrumento del PM falla, otra vez, y de tres formas** | Tercera fila de esta familia (`Ola 3 M1 · sed`, fila 82). (1) El PM verificó la superficie de APIs del Apply con `git diff <archivo>` sobre un archivo en estado `MM`: eso muestra **solo lo no-staged**, así que revisó 12 líneas creyendo revisar 255 y se le escapó `glib.timeoutAddOnce`, que cazó el auditor. (2) El PM dejó una edición suya sin commitear (`pub const kelpie_css`) y entregó el árbol a QA, que la borró con un `git checkout --` legítimo al revertir un sabotaje; QA lo detectó y lo restauró, pero el fallo fue del PM. (3) El PM lanzó al auditor sobre un árbol que QA seguía escribiendo: su primera corrida midió un artefacto sucio y roja. | **Tres reglas mecánicas, ninguna opinable.** (1) Para leer lo que un commit realmente cambió, `git show <sha> -- <archivo>` o `git diff HEAD -- <archivo>`; **nunca `git diff <archivo>` a secas**, que miente en silencio sobre cualquier archivo con algo en el índice. (2) El PM commitea lo suyo **antes** de entregar el árbol a un subagente: un `git checkout --` correcto del subagente no puede distinguir su sabotaje de tu edición. (3) Al auditor se le entrega un **artefacto congelado**: commit hecho, árbol limpio, y decírselo en la orden. Si QA sigue escribiendo, el auditor arranca después. |
| 2026-08-31 | #16 (Ola C M1) | **Motor / segunda firma de muerte silenciosa, y el fallback ya no se discute** | OpenCode+MiMo falló los dos Apply con `exit 0` y **cero diff**, en 11 min y 5.5 min de un presupuesto de 2400 s. Descartadas las dos causas instrumentales del ledger (`#15`): el log no tenía basura de shell —el wrapper estaba bien— y no lo mató ningún `timeout`. La ronda 2 llevaba prompt reforzado con las 40+ firmas **inlineadas verbatim** y prohibición explícita de abrir los bindings; **obedeció** (solo leyó los seis archivos autorizados) y aun así murió a mitad de herramienta. El fallback Claude entregó, y de paso encontró un bloqueo de compilación preexistente de #12 que ningún Apply había llegado a destapar. | Confirma la causa (2) de la fila de `#15` y le pone umbral: **un log limpio que se corta a mitad de herramienta muy por debajo del presupuesto es muerte silenciosa del motor externo, y a la segunda vez en el mismo issue se va al fallback sin tercera ronda.** Reforzar el prompt ya se probó y no la evita: aquí el prompt reforzado se obedeció y el proceso murió igual. Lo que **no** se puede concluir sin leer el wrapper generado sigue vigente — pero una vez descartado, no hay que gastar más rondas buscando la frase mágica. |
| 2026-09-01 | #81 (Ola C M1) | **Ledger / la obligación tiene que cargar su propia evidencia** | La regla que salió de #16 —bajar cada fila vigente del ledger a una obligación del diseño— se aplicó: el diseño de #81 tiene cuatro. El auditor las evaluó una a una y **fallaron tres**. La 1 (contrato oculto) pedía "el Apply declara por qué caminos se llega a `stop()`" y se quedó en conectar la señal; el contrato oculto estaba en el doc-comment del binding que el propio diseño ya citaba (`gio2.zig:895`: `shutdown` se emite *después* de que la loop muere) y nadie lo leyó. La 3 (test que no discrimina) protegió el escenario que nombraba, y en el mismo archivo entró otro test tautológico. La 4 (gate de ejecución real) **se corrió, con el entorno del desarrollador, y salió verde sobre una app que panicaba al arrancar**. Solo se cumplió la 2, y el auditor identificó por qué: era la única cuya comprobación era inspeccionable en el diff —o está el `!void` o no está—. Las otras tres estaban escritas como intenciones («el Apply declara», «QA sabotea», «lo corre el PM») y nadie podía verificar después si se habían hecho. | **Bajar la fila a obligación es el paso 1 y funciona; falta el paso 2: cada obligación declara QUÉ RASTRO DEJA y dónde lo busca el auditor.** No qué hacer — qué queda escrito cuando se hizo. «Lo corre el PM» se escribe como «pega el comando exacto y su exit code en el reporte»: con el comando pegado, el auditor ve el entorno y el teatro se cae en un vistazo. «El Apply declara los caminos» se escribe como «lista los caminos a `stop()` en el doc-comment de `Link`», que es un grep. Y **si la fila del ledger es sobre una clase de defecto, la obligación es sobre la clase, no sobre un escenario**: la 3 debió cubrir *todos* los tests nuevos del diff —QA lista cada test nuevo con el sabotaje que lo vio en rojo—, porque el test que no tiene fila es el que no se probó. Sin rastro, la obligación es incomprobable y el único gate que la comprueba es el auditor adivinando. |
| 2026-09-01 | #81 (Ola C M1) | **Gate / el entorno del desarrollador esconde el bug que el gate busca** | `Link.start` hacía `@memcpy` del buffer de la ruta del socket sobre sí mismo: `client.resolveSocketPath` **construye la ruta dentro del buffer que recibe** y devuelve un slice de él en dos de sus tres ramas. Zig 0.16 aborta (`@memcpy arguments alias`), así que kelpie panicaba al arrancar en **cualquier instalación que no exporte `HERDR_SOCKET_PATH`** — la única rama que no aliasea. Esta máquina la exporta. Consecuencia: el builder no lo vio, los tres tests que QA escribió para el ciclo de vida **ponían `HERDR_SOCKET_PATH` y por tanto no podían cazarlo estructuralmente**, y el gate de ejecución real del PM —obligación 4 del diseño— habría pasado en verde sobre una app rota. Lo cazó el auditor leyendo `resolveSocketPath` y reproduciéndolo con `env -u`. | **Un gate de ejecución se corre con el entorno saneado, no con el del desarrollador, y todo lo que el código lea del entorno se prueba en sus dos estados.** En concreto: `env -u <VAR>` para cada variable que el camino bajo prueba consulte, y el comando exacto pegado en el reporte (ver la fila de arriba). Y la regla de test que salió de aquí, propuesta por QA al analizar su propio punto ciego: cuando una función devuelve un slice **del buffer que le pasas**, eso es un contrato, no un detalle — se fija con un test que compare punteros (`@intFromPtr` del slice contra el rango del buffer), no probando rutas. Un contrato sin escribir lo vuelve a pisar el siguiente caller. |
| 2026-09-01 | #81 (Ola C M1) | **Arnés / el scratchpad no sobrevive a la noche, y el PM casi culpa al motor** | Al reanudar el issue al día siguiente, un Apply salió con `exit 0` y cero diff. Iba a anotarse como la segunda muerte silenciosa de MiMo en el mismo issue —umbral que manda al fallback según la fila de `#15`—. Leyendo el wrapper: `apply-81-ui.sh: No such file or directory`, exit **127**. El `/tmp` se limpió entre sesiones y el script del arnés había desaparecido; el prompt sí sobrevivió. Cuarta vez en el ledger que un fallo de Apply parece del motor externo y es del PM, y la primera cuya causa es simplemente el paso del tiempo. | **Al reanudar un issue, los artefactos del arnés se recrean ANTES de tocar a ningún builder** — igual que el `Monitor` del fleet se re-arma en cada reanudación. Y la fila de `#15` se refuerza: antes de atribuir un `exit 0` sin diff al motor, el primer comando es mirar el log **por el principio**, no por el final; un exit 127 en la segunda línea se lee en un vistazo y ahorra una ronda entera. |
| 2026-09-01 | #81 (Ola C M1) | **Fleet / reincidencia: QA y auditor en paralelo sobre el mismo checkout** | El PM lanzó `qa-81` y `auditor-81` a la vez sobre la misma rama, y el auditor abrió su reporte avisando de que el árbol se había movido bajo sus pies: QA tenía ~150 líneas sin commitear. Es **reincidencia literal** de la preocupación 5 que el mismo rol levantó en #16, sobre la que el PM escribió «congelar el artefacto» y dijo que no se repetiría. No hizo daño porque el auditor lo detectó y emitió su veredicto sobre el commit, pero el aviso lo dio él, no el PM. | La regla ya existía y se incumplió, así que lo que falta no es la regla sino el mecanismo: **el auditor se lanza cuando QA ha terminado y su trabajo está commiteado, nunca en paralelo.** El paralelismo entre QA y auditoría no ahorra tiempo real —el auditor acaba auditando un artefacto que cambia— y cuesta una ronda de aclaración. Si hace falta paralelizar, es con el auditor sobre un commit y QA sobre una rama aparte, no sobre el mismo checkout. |
